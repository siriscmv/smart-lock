generator client {
    provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:db.sqlite"
}

model vehicles_location {
    vid String @id
    latitude Float
    longitude Float
}